<?php
/**
 * Origin 框架路由注册文件，以数组为信息表述方式
 * 路由结构由：route（路由地址），mapping（映射地址）
 * 例：
 * array(
 *     array(
 *          'route' => 'new/index',
 *          'mapping' => 'Home/new/index',
 *      ),
 * );
 */
return array();