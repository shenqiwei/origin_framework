<?php
/**
 * Origin 框架路由注册文件，以数组为信息表述方式
 * 路由结构由：name（路由名称）, route（路由地址），mapping（映射地址）
 * 例：
 * array(
 *     array(
 *          'name' => 'news',
 *          'route' => 'news/index',
 *          'mapping' => 'Home/news/index',
 *      ),
 * );
 */
return array();